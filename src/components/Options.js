import React, { useState } from 'react';

const Options = ({query}) => {
    const [collapse, setCollapse] = useState (true)

return (
    <div className = "optionsDiv">
        <div className = "optionsTab"> FILTER/SORT </div>

    </div>
)
};

export default Options;